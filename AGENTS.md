# AGENTS Instructions

- For this repository, static build artifacts for GitHub Pages **must** be generated under `docs/`.
- When changing build scripts or CI workflows, preserve behavior so that a build command produces deployable files in `docs/` (GitHub Pages publish root).
- Do not switch the publish output directory away from `docs/` unless explicitly requested by the user.

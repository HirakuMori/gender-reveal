{
  "name": "gender-reveal-static",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "build": "npm run build:pages",
    "export": "BASE_PATH=/gender-reveal NEXT_PUBLIC_BASE_PATH=/gender-reveal next build && rm -rf docs && mv out docs",
    "start": "next start",
    "lint": "next lint",
    "build:pages": "BASE_PATH=/gender-reveal NEXT_PUBLIC_BASE_PATH=/gender-reveal next build && rm -rf docs && mv out docs",
    "build:root": "BASE_PATH= NEXT_PUBLIC_BASE_PATH= next build && rm -rf docs && mv out docs"
  },
  "dependencies": {
    "next": "15.3.2",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "@types/node": "22.15.17",
    "@types/react": "19.0.10",
    "@types/react-dom": "19.0.4"
  }
}
